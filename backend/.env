
- # Server Configuration
+ #############################
+ # Backend environment file  
+ # IMPORTANT: Replace placeholders below with real secrets
+ # Do NOT commit real credentials to git. Use Render's Environment
+ # variables for production instead.
+ #############################
+ 
+ # Server
  PORT=5000
- NODE_ENV=development
- 
- # JWT Secret
- JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-123456789
- 
- # Database Configuration
- MONGO_URI=mongodb://127.0.0.1:27017/xapobank
- 
- # Bitcoin API (CoinGecko - Free tier)
+ NODE_ENV=production
+ 
+ # JWT (use a long random string)
+ JWT_SECRET=CHANGE_ME_TO_A_STRONG_RANDOM_SECRET
+ 
+ # MongoDB (Atlas) - replace <USER> and <PASS> with your credentials
+ # Example: mongodb+srv://alice:pa$$word@cluster0.mongodb.net/xapobank?retryWrites=true&w=majority
+ MONGO_URI=mongodb+srv://<MONGO_USER>:<MONGO_PASS>@cluster0.mongodb.net/xapobank?retryWrites=true&w=majority
+ 
+ # Backend runtime origin used by /config.js (optional override)
+ BACKEND_ORIGIN=https://backend-xapobank.onrender.com
+ 
+ # Frontend URL (used for some templates/CSP in dev)
+ CLIENT_URL=https://your-frontend.onrender.com
+ 
+ # Promo codes (comma-separated). You can also manage via admin API on the backend.
+ PROMO_CODES=XAPO-H7K2M9QZ
+ 
+ # Bitcoin pricing API
  BITCOIN_API_URL=https://api.coingecko.com/api/v3
  
- # Frontend URL (for CORS)
- FRONTEND_URL=http://localhost:5000
- 
- # Email Configuration (SMTP)
- SMTP_HOST=smtp.gmail.com
+ # Email (SMTP) - fill in if you want outbound emails
+ SMTP_HOST=smtp.example.com
  SMTP_PORT=587
- SMTP_EMAIL=your-email@gmail.com
- SMTP_PASSWORD=your-app-password
+ SMTP_USER=your-smtp-user
+ SMTP_PASS=your-smtp-password
+ NOTIFY_FROM=notify@yourdomain.com
  
- # Rate Limiting
+ # Rate limiting (simple config)
  RATE_LIMIT_WINDOW=15
  RATE_LIMIT_MAX_REQUESTS=100
  
- MONGO_URI=mongodb+srv://USER:PASS@cluster0.mongodb.net/xapobank?retryWrites=true&w=majority
+ # Notes:
+ # - On Render, set these values in the service's Environment page instead of committing this file.
+ # - If you prefer local testing, update this file with real (local or Atlas) credentials.
  
  
